from django.urls import path
from . import views

urlpatterns = [
    path('', views.index, name='index'),
    path('authen', views.auth_view, name='auth'),
    path('check', views.check_view),
    path('index', views.index),
    ]